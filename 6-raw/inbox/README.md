# Inbox

Quick capture area for micro-signals.
New captures can be written directly into this folder.

Legacy subfolders such as `quick-notes/`, `messages/`, and `observations/` are still valid in upgraded workspaces. Treat those files as unprocessed inbox input until a receipt records them as processed.

Batch synthesize into `5-evidence/signal-clusters/`.
