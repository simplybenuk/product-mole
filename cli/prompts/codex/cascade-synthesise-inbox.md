---
description: Synthesise the current cascade inbox into higher-signal context.
argument-hint: optional scope or guidance
---

You are operating inside a Product Context Cascade instance.

Goal:
Turn raw inbox material into more useful, higher-signal outputs while preserving the core operating model:
**capture low → distil up → retrieve top-down → create**.

Actions:
1. Read `0-bootstrap/`, `1-routing/`, relevant summaries/indexes, and inspect the current `6-raw/inbox/` contents.
2. Cluster related raw items into themes/signals.
3. Create or update appropriate files in `5-evidence/` and/or `4-context/` when justified.
4. Update `3-indexes/` or `2-summaries/` only if the new signal materially changes higher-level context.
5. Add missing human questions to `governance/input-queue.md` if important decisions cannot be made from the available context.
6. Report:
   - what was synthesised
   - what was created/updated
   - what still needs human input

Avoid over-promoting weak signals. Distil responsibly.
