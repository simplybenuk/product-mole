2026-05-13

- Created initial Ralph planning scaffold for this repository.
- Current PRD focus is the Mole working-instance lifecycle and command surface.
- No Ralph PRD tasks have been completed yet.
- Existing source context used for the initial scaffold:
  - README.md
  - docs/vision/open-source-product-direction.md
  - plans/working-instance-upgrade-plan.md
  - docs/cli-and-command-surface.md
  - cli/README.md

2026-05-13

- Completed `instance-lifecycle-001`: expanded `mole doctor` to report source version, instance version, and missing metadata warnings.
- Added a Node test harness for the CLI and covered doctor output with and without `mole.instance.yaml`.
- Validation: `npm test` from `cli/` passed.
- Next priority: `instance-metadata-and-doctor` / `instance-lifecycle-002`.

2026-05-13

- Completed `instance-lifecycle-002`: added `upgrade-ownership.json` with safe-copy, merge-carefully, and never-overwrite classes.
- Documented the ownership classes in `docs/upgrade-ownership.md`.
- Added a validation test that parses the manifest and confirms `4-context/`, `5-evidence/`, and `6-raw/` are never-overwrite paths.
- Validation: `npm test` from `cli/` passed.
- Next priority: `upgrade-report` / `instance-lifecycle-003`.

2026-05-13

- Completed `instance-lifecycle-003`: replaced the placeholder `check-updates` command with a read-only report.
- The report compares source and instance versions, reports update status, and lists safe-copy paths separately from merge-carefully paths.
- Added tests for up-to-date and source-newer instance states.
- Validation: `npm test` from `cli/` passed.
- Category `instance-lifecycle` is complete. Next priority category: `command-surface`.

2026-05-13

- Completed `command-surface-001`: normalized CLI help and visible command text around `Mole` for the product name and `mole` for command examples.
- Added a help-output test covering the main command examples and preventing `Cascade` regressions in help text.
- Validation: `npm test` from `cli/` passed; `node cli/mole.mjs --help` produced the expected help text.
- Category `command-surface` is complete. No incomplete PRD tasks remain.

2026-05-13

- Completed `team-collaboration-001`: documented the shared synced-folder team operating model in the README.
- Added provider guidance for SharePoint/OneDrive, Google Drive for desktop, Dropbox desktop app, and Box Drive.
- Clarified that many contributors can capture raw inputs while one maintainer or agent should process and promote inbox material at a time.
- Validation: `jq empty plans/prd.json` passed; `npm test` from `cli/` passed.
- Next priority: `team-safe-capture` / `team-collaboration-002`.

2026-05-13

- Completed `team-collaboration-002`: added a shared capture filename helper used by both CLI capture and the local UI capture endpoint.
- Capture filenames now include a UTC timestamp, slug, and unique suffix, and writes refuse silent overwrite.
- Added tests for repeated similar notes and UI capture paths.
- Validation: `npm test` from `cli/` passed.
- Next priority: `team-safe-capture` / `team-collaboration-003`.

2026-05-13

- Completed `team-collaboration-003`: added `captured_by` metadata to CLI and UI raw captures.
- CLI capture defaults attribution from `MOLE_CAPTURED_BY`, then local username, then `unknown`; the UI now exposes a `Captured by` field.
- Added tests for attribution defaults and emitted frontmatter.
- Validation: `npm test` from `cli/` passed.
- Next priority: `inbox-processing-coordination` / `team-collaboration-004`.

2026-05-13

- Completed `team-collaboration-004`: documented shared inbox states for `new`, `processing`, `processed`, and `archive`.
- Updated agent guidance to treat legacy direct inbox folders as unprocessed input and never delete raw inputs before a retrieval receipt exists.
- Documented weak signal and substantive artefact movement through the state flow while preserving compatibility with current capture paths.
- Validation: `npm test` from `cli/` passed; state documentation checks passed with `rg`.
- Next priority: `inbox-processing-coordination` / `team-collaboration-005`.

2026-05-13

- Completed `team-collaboration-005`: added a file-based inbox processing lock and JSON receipt convention.
- Added `mole inbox claim [processor]` and `mole inbox complete [summary]` commands.
- Documented stale-lock handling and updated shared inbox agent guidance.
- Added tests for one active claim, safe concurrent-claim failure, receipt creation, and lock release.
- Validation: `npm test` from `cli/` passed; `node cli/mole.mjs --help` lists the inbox commands.
- Category `team-collaboration` is complete. No incomplete PRD tasks remain.

2026-06-11

- Completed `molehill-metrics-001`: added the processed inbox metrics data foundation under `governance/metrics`.
- Added `lib/metrics.mjs` with UTC daily counting, same-day path dedupe, starter file creation, ISO week rollups, calendar month rollups, and retention trimming.
- Added tests for unique counting, same-day dedupe, UTC date reset, daily retention, weekly/monthly retention, and preserving historical rollups.
- Validation: `npm test` from `cli/` passed.
- Next priority: `metrics-inbox-integration` / `molehill-metrics-002`.

2026-06-11

- Completed `molehill-metrics-002`: extended `mole inbox complete` with repeated `--processed <path>` support.
- Completion receipts now preserve processed paths and successful completions update local metrics after the receipt is written.
- Metrics update failures are reported as warnings and do not fail completion or prevent lock release.
- Validation: `npm test` from `cli/` passed.
- Next priority: `metrics-dashboard` / `molehill-metrics-003`.

2026-06-11

- Completed `molehill-metrics-003`: added the static Molehill Metrics dashboard at `governance/metrics/dashboard.html`.
- The dashboard reads daily, weekly, and monthly metrics JSON, supports view switching and date filtering, and displays totals, averages, latest values, a table, and a simple bar chart.
- Updated workspace scaffolding so new Mole workspaces include the dashboard and starter metrics files.
- Validation: `npm test` from `cli/` passed.
- Next priority: `metrics-guidance` / `molehill-metrics-004`.

2026-06-11

- Completed `molehill-metrics-004`: documented the processed-path metrics workflow in README, CLI docs, inbox synthesis skill guidance, and bootstrap agent rules.
- Updated `mole synthesise inbox` output so generated agent instructions include `mole inbox complete --processed <path> ... "summary"`.
- Validation: documentation checks with `rg` passed; `npm test` from `cli/` passed.
- Category `molehill-metrics` is complete. No incomplete PRD tasks remain in the category.

2026-09-12

- Final independent `$bwh-agent-review` for issue #23: **NOT READY FOR HUMAN TESTING**.
- The prior migration fixes are verified: YAML `source_refs` list entries (`- path:`) receive an indentation-preserving `source_id`, and migration reports include registry findings. Focused tests pass for both behaviors.
- Blocking data-integrity finding remains: `findRegistryFindings` checks only whether a record's `current_path` exists and does not hash it against `record.content_hash` (`lib/source-registry.mjs`). `migrateSourceReferences` includes that scanner but does not independently block conflicted sources (`lib/source-migration.mjs`). A temporary workspace changed a registered file from `before` to `after`: `resolveSource` returned `content_conflict`, while registry and migration findings were empty and `--apply` still added `source_id` to a structured reference. The scanner must emit `content_conflict`, and apply mode must not change references whose source is in conflict; add focused regression coverage.
- Validation: `npm test` passed 59/59 across 21 suites; affected Node syntax checks passed; package, PRD, schema, and ownership JSON parsed; `git diff --check` passed; a clean temporary scaffold passed `mole doctor`, `mole inbox audit`, and migration dry-run. The specification remains `IN DEVELOPMENT`; return to development for the conflict guard and rerun this review before human output testing or publication.

2026-09-12

- Follow-up `$bwh-agent-review` found one remaining blocking data-integrity issue; the verdict remains **NOT READY FOR HUMAN TESTING**.
- Blocking finding: `findRegistryFindings` checks whether `current_path` exists but never hashes it against `record.content_hash` (`lib/source-registry.mjs:851-870`). `mole source migrate` includes that scanner's output (`lib/source-migration.mjs:338-370`), so a modified registered file produces no `content_conflict` finding. A focused temporary-workspace reproduction changed a registered file from `before` to `after`; `resolveSource` correctly returned `content_conflict`, but `findRegistryFindings` and migration returned no finding. With a structured reference present, migration `--apply` still added `source_id` despite the unacknowledged content change. The scanner/report must emit `content_conflict` and migration must not apply a reference whose source record is in conflict; add focused regression coverage.
- Follow-up validation: `npm test` passed 59/59 across 21 suites; all affected modules passed `node --check`; package, PRD, schema, and ownership JSON parsed; `git diff --check` passed; temporary scaffold `mole doctor`, `mole inbox audit`, and migration dry-run passed. The prior YAML list and findings fixtures also pass.
- The specification remains `IN DEVELOPMENT`; return to development for the conflict guard, then rerun this independent review before human output testing or publication.

2026-09-12

- Completed GitHub issue #23 implementation on `codex/issue-23-stable-source-ids-spec`.
- Added the versioned source-record schema and file-native registry with immutable `src_` UUIDv4 IDs, exact-byte SHA-256 hashes, path and content history, attachment links, correction history, duplicate/conflict findings, and workspace/symlink safety checks.
- Integrated source registration into CLI and UI captures, added `mole source register|resolve|reconcile|correct|migrate`, and preserved source bytes during registration.
- Added ID-bearing `processed_sources` receipt entries while retaining legacy `processed` paths, source-ID-aware inbox audit and metrics deduplication, structured source-reference guidance across templates, and scaffold/upgrade ownership rules.
- Added dry-run-first legacy migration with resolved, ambiguous, and unresolved classifications. Apply mode only updates resolved structured references and writes before/after hashes to an audit report.
- Validation: `npm test` passed with 57 tests across 21 suites; Node syntax checks passed; package, PRD, and schema JSON parsed; `git diff --check` passed.
- Implementation handoff: independent `$bwh-agent-review` review required before the spec can move from `IN DEVELOPMENT` to `READY FOR HUMAN TESTING` and before publication.

2026-09-12

- Independent `$bwh-agent-review` for issue #23: **NOT READY FOR HUMAN TESTING**.
- Blocking finding: migration does not recognise the normal YAML sequence form used by `source_refs` (`- path: ...`). `lib/source-migration.mjs:20` only matches a line beginning with `path:`, and the YAML discovery loop at `lib/source-migration.mjs:230-236` therefore misses list entries. A temporary workspace with a registered source and `source_refs:\n  - path: ...` classified only the path-text occurrence and `--apply` reported `changed: 0`, leaving the structured YAML reference without `source_id`. Fix YAML sequence/map recognition, preserve valid indentation, and add an apply-mode YAML fixture before human testing.
- Should-fix finding: `migrateSourceReferences` reports references and changed artifacts but no `findings` collection and does not include the actionable duplicate, conflict, orphan, or revision findings returned by the registry scanner (`lib/source-migration.mjs:318-356`). The registry API detects several of these cases, but the migration report and CLI do not surface them as required by the migration persistence contract and AC14. Include the findings in the report and add focused coverage.
- Validation evidence: `npm test` passed 57/57 across 21 suites; all affected Node modules passed `node --check`; package, PRD, schema, and ownership JSON parsed; `git diff --check` passed; a clean scaffold passed `mole doctor`, `mole inbox audit`, and migration dry-run. Repository-root `mole inbox audit` was not applicable because the source repository is not itself a Mole instance (`mole.instance.yaml` is absent).
- Review artifact and state: this entry is the review record; the specification remains `IN DEVELOPMENT`. Human output testing is blocked until the migration fixes and focused tests land. Afterward, re-run the independent review; if ready, focus human testing on CLI/UI capture parity, archive resolve/reconcile, correction conflicts, attachment reciprocity, receipt and metrics compatibility, and dry-run/apply migration safety. A successful human test hands the change to `bwh-archive-change`; a failure returns it to `bwh-development`.

2026-06-11

- Completed `molehill-metrics-005`: added explicit historical metrics backfill with `mole metrics backfill`.
- Backfill reads `governance/run-receipts/inbox-processing/*.json`, counts only receipt `processed` paths with valid `completed_at` dates, dedupes per UTC day, and rebuilds daily, weekly, monthly, and current-day dedupe metrics.
- Documented backfill for upgraded existing workspaces and clarified that it does not infer from raw folders or read raw insight content.
- Validation: `npm test` from `cli/` passed; documentation checks with `rg` passed.
- Category `molehill-metrics` is complete. No incomplete PRD tasks remain in the category.

2026-09-12

- Final independent `$bwh-agent-review` re-review for GitHub issue #23: **READY FOR HUMAN TESTING**.
- Verified the conflict remediation: registry findings now hash existing `current_path` bytes and emit actionable `content_conflict`; migration demotes references to conflicted sources to `unresolved`, so apply mode leaves them unchanged. Focused registry and migration regressions pass.
- Validation: `npm test` passed 60/60 across 21 suites; affected Node syntax checks passed; package, PRD, schema, and ownership JSON parsed; `git diff --check` passed; a clean temporary scaffold passed `mole doctor`, `mole inbox audit`, and migration dry-run. A direct changed-byte reproduction produced a registry/migration `content_conflict`, zero applied changes, and no `source_id` insertion.
- Human testing focus: verify CLI/UI capture parity, archive resolve/reconcile, correction conflict handling, attachment reciprocity, receipt and metrics compatibility, and dry-run/apply migration safety including resolved, ambiguous, unresolved, YAML, and changed-byte references. Successful human testing hands the accepted change to `bwh-archive-change`; findings return it to `bwh-development`.

2026-09-12

- Addressed PR review thread 3997607752 in `lib/source-migration.mjs`: a basename match is no longer promoted by a resolver result that only verifies an existing current path. The resolver fallback remains available for a unique exact-content-hash move, while exact path, path-history, and date evidence are unchanged.
- Added focused migration regressions proving a missing reference with a basename-only candidate stays `unresolved` and `--apply` makes zero changes, while a moved source still resolves through exact content-hash evidence.
- This is review remediation of the completed `source-provenance-004` task; no new PRD task was created. Assumption: a registry's valid current path is not evidence that an unrelated missing reference with the same basename identifies that source.
- Validation: `npm test` passed 62/62 across 21 suites; the focused migration suite passed 8/8; affected syntax checks and `git diff --check` passed. Next handoff: an independent `$bwh-agent-review` for the remaining PR review comments.

2026-09-12

- Independent `$bwh-agent-review` of PR review thread 3997607752: **READY FOR HUMAN TESTING** for this bounded remediation.
- Verified `candidateForReference` no longer treats a basename-only legacy reference as resolved when `resolveSource` merely confirms the registered record's existing `current_path`; the fallback now requires resolver evidence of a unique `exact_content_hash` match at a different path and rejects the `current_path`+hash evidence combination.
- Focused migration tests pass for the required cases: a missing `6-raw/archive/note.md` with only a registered `note.md` basename remains `unresolved`, `migrateSourceReferences(..., { apply: true })` reports zero changes and leaves the JSON reference byte-for-byte unchanged; a moved source with unique exact-content-hash evidence remains `resolved`. Exact path/path-history handling remains in the direct lookup branch and is unchanged.
- Validation: focused `node --test lib/test/source-migration.test.mjs` passed 8/8; root `npm test` passed 62/62 across 21 suites; affected `node --check` and `git diff --check` passed. No blocking or should-fix findings for thread 3997607752. Other PR review threads remain outside this bounded review and require separate development/review cycles.
- Human testing focus for this remediation: verify `mole source migrate --apply` does not add an ID to a basename-only missing reference, while exact-path/path-history and unique content-hash move references remain eligible for migration. Successful complete PR review/testing hands the accepted change to `$bwh-archive-change`; any finding returns it to `$bwh-development`.
2026-09-12

- Addressed PR review thread 3997607759 in lib/source-migration.mjs: structured migration discovery and apply writes are now limited to the documented source_refs and processed_sources containers. Generic path-text references remain report-only, and YAML structured writes are limited to those same containers.
- Added focused coverage proving an unrelated JSON output.path remains unchanged and receives no source_id, while a resolved source_refs entry is migrated. Existing exact-path, moved-source, YAML list, conflict, and findings fixtures remain covered.
- Assumption: source_id frontmatter, legacy processed path arrays, source-record fields, and arbitrary objects containing path are not source-reference containers; preserving their values is required by the issue #23 contract.
- Validation: npm test passed 63/63 across 21 suites; focused migration tests passed 9/9; affected syntax check and git diff --check passed. No commit or remote action was performed.
- Next handoff: independent bwh-agent-review for this bounded PR comment remediation.

2026-09-12

- Independent `$bwh-agent-review` of PR review thread 3997607759: **READY FOR HUMAN TESTING** for this bounded remediation.
- Verified JSON discovery and apply writes are restricted to the documented `source_refs` and `processed_sources` containers. Generic path-bearing objects such as `output.path` remain report-only and are unchanged during apply; valid resolved `source_refs` entries receive the selected `source_id`.
- Verified YAML structured writes use the same container boundary, and manually exercised `processed_sources` migration while preserving legacy `processed` paths and unrelated `output.path` values. No blocking or should-fix findings for thread 3997607759; the other PR threads remain outside this bounded review.
- Validation: focused `node --test lib/test/source-migration.test.mjs` passed 9/9; root `npm test` passed 63/63 across 21 suites; manual JSON/YAML boundary checks passed; affected `node --check` and `git diff --check` passed.
- Human testing focus for this remediation: run `mole source migrate --apply` against JSON and YAML receipts/artifacts containing `source_refs` and `processed_sources`, confirm unrelated `output.path` and legacy `processed` values are untouched, and confirm resolved structured entries receive IDs. Successful human output testing hands the accepted change to `$bwh-archive-change`; any finding returns it to `$bwh-development`.

2026-09-12

- Addressed PR review thread 3997607763 in `lib/inbox-audit.mjs`: an ID-bearing receipt path is now only an audit fallback for candidates without a source ID, or for a historical `processed` path that is not represented by an ID-bearing entry. Candidates with source IDs require a matching processed source ID or that scoped legacy path fallback, so a path hint for source A cannot mark source B processed.
- Added focused coverage in `cli/test/mole.test.mjs` for source A's ID-bearing path hint being reused by source B; the existing moved-source and mixed/legacy receipt compatibility cases remain covered and passing.
- Assumption: in a mixed receipt, a `processed` path not repeated in `processed_sources` is still a genuine path-only compatibility item; a path repeated by an ID-bearing entry is not. This preserves existing receipt-reader behavior while preventing cross-source path attribution.
- Validation: focused inbox-processing tests passed 13/13; root `npm test` passed 64/64 across 21 suites; `git diff --check` passed. No commit or remote action was performed.
- Next handoff: independent `$bwh-agent-review` for this bounded PR comment remediation. The separate migration `current_path` review thread remains out of scope.

2026-09-12

- Independent `$bwh-agent-review` of PR review thread 3997607763: **READY FOR HUMAN TESTING** for this bounded inbox-audit remediation.
- Verified `auditInbox` uses a discovered candidate `source_id` as the identity when an ID-bearing receipt supplies only a path hint; a mismatched source ID at the same path remains unprocessed. A matching processed source ID still counts, including after the file moves. Historical path-only receipts remain a compatibility fallback, while a duplicate path in the same ID-bearing receipt entry is not treated as legacy evidence.
- Validation: focused inbox-processing tests passed 13/13 (including mismatched-ID, moved-source, mixed receipt, and legacy path cases); root `npm test` passed 64/64 across 21 suites; `node --check` passed for the affected module and test file; `git diff --check` passed. No blocking or should-fix findings for this thread. The separate migration `current_path` review thread remains outside this bounded review.
- Human testing focus for this remediation: run `mole inbox audit` with an ID-bearing receipt whose path hint is reused by a different source, then verify the different source remains listed as unprocessed; verify matching IDs still suppress moved files and historical path-only receipts still suppress their paths. Successful human output testing hands the accepted change to `$bwh-archive-change`; any finding returns it to `$bwh-development`.
